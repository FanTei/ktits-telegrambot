version: '3.8'

services:
  kstb-db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: '0520'
      MYSQL_DATABASE: 'dev_kstb_db'
      MYSQL_USER: 'kstb_db'
      MYSQL_PASSWORD: 'kstb'
    ports:
      - "3308:3306"
    expose:
      - '3308'